version: 2
updates:
  # Auto-bump pinned GitHub Actions (SHA pins → readable PRs with diff).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
