import type { IngredientInput } from '../types'

// Skeleton: haircare ingredient data removed (lives in the SQL snapshot). Add
// category files and spread them here to seed haircare ingredients from TS.
export const haircareIngredients: IngredientInput[] = []
