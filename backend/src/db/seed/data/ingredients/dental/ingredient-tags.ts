import type { IngredientTagMap } from '../../ingredient-tags'

// No dental tag mappings yet. When adding one, import the slug from
// `./ingredient-slugs` and the tag from `../../tags`, keyed by `DENTAL_*.X`.
export const dentalTagMap: IngredientTagMap = {}
