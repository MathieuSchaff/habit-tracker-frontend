import type { IngredientInput } from '../types'

// Skeleton: dental ingredient data removed (lives in the SQL snapshot). Add
// category files and spread them here to seed dental ingredients from TS.
export const dentalIngredients: IngredientInput[] = []
