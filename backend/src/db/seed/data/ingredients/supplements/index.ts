import type { IngredientInput } from '../types'

// Skeleton: supplement ingredient data removed (lives in the SQL snapshot). Add
// ingredient files and spread them here to seed supplements from TS.
export const supplementIngredients: IngredientInput[] = []
