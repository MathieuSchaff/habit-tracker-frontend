import type { IngredientTagMap } from '../../ingredient-tags'

// Skeleton: supplement ingredient→tag associations removed (live in the SQL
// snapshot). Re-add entries keyed by ingredient slug to seed them from TS.
export const supplementTagMap: IngredientTagMap = {}
