{
  // Solution file: routes each backend file to its build (src) or test (*.test.ts) project,
  // so the editor gives test files bun:test types instead of an inferred typeless project.
  "files": [],
  "references": [{ "path": "./tsconfig.build.json" }, { "path": "./tsconfig.test.json" }]
}
